{
  "scope": "technical_backend_defaults_not_scientific_protocol_approval",
  "architecture": {
    "width": 64,
    "heads": 4,
    "layers": 2
  },
  "fit_settings": {
    "steps": 1000,
    "batch_size": 128,
    "learning_rate": 0.0003,
    "checkpoint_every": 100,
    "heartbeat_every": 20
  },
  "notes": [
    "No dataset role, main time horizon, seed or baseline floor is chosen by this file.",
    "Every real run requires a separately approved hash-bound experiment protocol.",
    "Fixed-budget forecast fitting is not development selection, calibration or confirmation.",
    "DataLoader multiprocessing, Stage5C and SMC are not enabled."
  ]
}
