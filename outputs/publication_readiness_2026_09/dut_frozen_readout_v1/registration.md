# Frozen DUT Calibration-Domain Readout

Registered before any DUT forecasts or forecast errors in this experiment.
Routine research review is delegated by the author. This is an explicit scoped
readout decision, not an assertion that source reservations alone grant access.
Use the existing source-use disposition and limited row-quality audit; exclude
`dut_intersection_04` without inspecting its predictive performance.

## Scientific Scope

The fixed source-only producer chain excludes DUT. The pinned authors describe
two Dalian campus locations, distinct from the Stanford source study. Historical
project-wide predictive exposure is not universally proven absent; do not erase
the original metadata's unknown-history declaration. This readout is descriptive
calibration-domain evidence, not untouched confirmation, fitting, a new model
selection set, or an independent generalization certificate. DroneCrowd remains
entirely confirmation-reserved and is not opened by this runner.

Use raw positions under an offline-annotation observation contract. Source
interpolation/control-point provenance is not equivalent to online sensor-time
availability. Upstream filtered velocities are forbidden. No units conversion,
metric calibration, seconds, equal physical horizon, or physical safety claim.

## Fixed Population and Models

All 27 non-quarantined recordings, two physical sites, both pedestrian and vehicle
agents. Query every observed integer frame from the first observed frame plus
seven. Observe eight native annotation frames, predict twelve, stride one. Retain
every currently visible context agent and every complete-past target regardless
of whether its future labels exist. Do not use the future-conditioned episode
index for query/agent eligibility. This is the full specified query population,
not a quick sample. Source SDD models used stride twelve, so temporal transport
is a substantive limitation, not a matched-seconds comparison.

Keep all twelve pre-existing predictor/head/seed views, all eleven controller
arms, and seven fixed causal baselines. No retraining, model selection, threshold
search, temperature fit, or external normalization. Models/controls remain bound
to external_policy_chain_v1. Chunk 128 queries, predictor batch128; graph decisions
remain per original scene. Batching can change floating-point rounding relative
to single-query inference, so the execution configuration is frozen and replayed.

## Outcomes and Missingness

Primary descriptive outcome: complete-path native ADE, equal weight to each of
the two sites; within a site weight complete target windows equally. Also FDE,
past-normalized ADE, per-recording/per-agent-type errors, and switch counts.
Easy/hard use the previously declared source-only past-normalized CV ADE cuts
0.02349376610737637 / 0.1679398244819204. Neither is refitted on DUT. Report
exact-zero easy separately. The empirical easy limit remains2%, not a guarantee.

Inference is never filtered by future support. Report complete, partial and
missing support. For the full past-eligible population, each missing future step
contributes an interval of plus/minus candidate/CV disagreement to the ADE gain.
Report the aggregate lower/upper gain bounds, not invented absolute labels.
Unknown-context interaction edges remain unpriced and must be counted.

Primary mechanism contrast remains half-count joint versus unary geometry,
same reference count and predicted harm budget. Failed solver matching is
reported, not discarded as a success. Predicted risk matching is not equal realized
risk. Full-count arms can have a unique assignment and cannot demonstrate coupling.

Two locations cannot support a tight population-risk or meaningful independent-
site generalization claim. A3000-resample paired two-site bootstrap is descriptive
only, with its coarse support and limitation explicit. Do not bootstrap windows
or treat27recordings as27IIDsites. No risk threshold or deployable arm will be
selected from these results; an unsupported learned policy remains uncertified.

## Execution and Recovery

Native arm64 CPU4/interop1/workers0. Save atomic128-query progress per recording,
hash-bound to code/config/source/model chain; retain completed-record receipts.
Resume unchanged run without choosing a new model or sample budget. Record PID,
elapsed time and progress. Replay first/middle/last128-query chunks per recording
in a fresh process, including input, prediction, decision and label identities.
This replay is engineering verification, not another independent experiment.

Public outputs contain aggregate results, protocol/code/hashes only. Raw
annotations, cached histories, per-query receipts and checkpoints stay local.
The ordinary deployment remains unchanged. Stage5C execution and SMC stay off.
