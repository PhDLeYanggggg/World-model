# Fixed Cost-Fit Support Diagnosis

This diagnostic follows the failed primary EqMotion cost-refit comparison; it
is not prospective confirmation of a new method. It uses the same four exposed
source sites and frozen checkpoints, with no fitting, threshold selection,
deployment or new data roles.

Before this diagnostic readout, fix support strata to the positive-value
q50/q90/q99 of forecast disagreement and observed last-step displacement in
each head's COMPLETE TRAINING rows. Zero is a separate group. Apply those exact
boundaries to fitting and held-source rows. Report every arm/view/population,
complete outcome counts, unknown/incomplete counts, native and disagreement-
fraction MSE, benefit/harm bias and descriptive rank correlation. Also report
fixed net/strict policy-eligible slices and per-inner-training-source slices.

Null predictors use only training-weighted means: constant native costs and
disagreement times mean cost fractions. They are fit-quality comparators, not
new intervention policies. In-fit head evaluation is explicitly optimistic;
its predictor inputs were nevertheless generated with both required source
exclusions. Contrast with held-source results cannot isolate predictor training
size (two versus three sites), covariate shift or conditional-label shift.

Use complete supervision only for numerical cost comparisons. Unknown labels
never become zero harm. Read label arrays only for diagnostics, never for the
frozen choice interface. No bins, policies or primary endpoint will be selected
from the findings. This analysis can motivate a separately registered repair.

Eight observed/twelve predicted sampled annotation steps, SDDstride12,
annotation pixels only; no seconds, metric, independent safety, true3D or
foundation claim. Stage5C/SMC remain off.
