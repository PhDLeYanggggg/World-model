# Statistical Scope

- Primary: the fixed region-weighted strict head versus the frozen equal-budget
  intermediate strict head. The positive ADE contrast is necessary but not
  sufficient; every scene/seed easy-degradation requirement also applies and fails.
- Four physical source sites,33recordings,175,756causally eligible forecast rows.
  Labels for2,799rows lack every future ADE point and remain unknown. Three model
  seeds do not create12independent sites. The selected counts repeat queries by seed.
- Bootstrap3,000 paired resamples uses four sites, not overlapping windows. It
  quantifies conditional development uncertainty with few clusters, not fresh
  generalization or a population-safety guarantee. Sites have informed prior design.
- All forecast outputs and decisions were frozen before this aggregate readout.
  Old in-sample cost-fitting selections define the registered training weight.
  They are supervised fitting tools, not independent calibration examples.
- Positive-easy and hard thresholds come from complete fitting outcomes. These
  are diagnostic subgroup definitions, not deployment inputs. The positive-easy
  diagnostic excludes exact-zero CV, whose absolute harm is reported separately.
- Missing outcomes are not zero-error or safe cases. Complete-only cost diagnostics
  and observed-outcome ADE have different supports; full-grid bounds retain missing
  outcomes explicitly. gates lower bounds are negative in two seeds.
- The new strict rule intervenes more often. Its equal-count contrast is negative,
  so higher fixed-rule accuracy does not demonstrate better ranking at common volume.
- Secondary policies/control contrasts and failure slices are descriptive and
  unadjusted. No favorable secondary policy is promoted after the failed primary.
- The single fixed multiplier has not been tuned after readout. Architecture and
  budget match the predecessor, but repeated development still consumes evidence.
- Replay and separate arithmetic are engineering validation by the same agent,
  not new seeds, independent replication or external confirmation.
- Continuous predicted benefit/harm are not calibrated failure probabilities.
  Changing fitting allocation supplies no conformal, subgroup or physical-safety
  guarantee. Dataset coordinates remain annotation pixels, not verified meters/time.

Conclusion: a reproducible developmental accuracy gain, a failed local protection
repair, and unresolved independent calibration/confirmation. No deployment or
submission-readiness claim follows.
