# Frozen Score-Head Scaling Check

## Material Passport

Fresh diagnostic of54 existing gain/risk heads on the twelve opened European
Squares source localities. Up to512 evenly spaced rows per fitting/excluded
locality, not random independent examples. No new training, selection or
deployment. Full source/producer identities and checkpoint hashes are recorded.

No sampled excluded row activates a training-constant feature by more than one
standardized unit. The largest absolute standardized coordinate is34.04, not
the hypothesized1e6 amplification. Neural all-event harm logits below-10 occur
in0--0.59% of sampled excluded rows. Utility/easy heads have more saturation,
but this diagnosis does not support universal output collapse as the cause.
Sampling cannot rule out rarer unsampled extremes or selected-row failures.

The hypothesis of a constant-feature numerical explosion is not supported here;
there is no justification for silently changing the historical normalizer.
Standardization remains training-only. General cautions about outliers and
zero-variance scaling do not prove a defect in this run.
[Primary API reference](https://scikit-learn.org/stable/modules/generated/sklearn.preprocessing.StandardScaler.html).

The next controlled refit instead makes predicted gain/harm depend explicitly
on a causal bound from candidate-versus-baseline displacement. It retains the
existing forecasts,features,scaler,loss,budget and sampler, and separately tests
utility-only,risk-only and both-head replacements. This is a hypothesis to test,
not a correction already shown effective.

Image pixels,obs8/pred12 rawstride12,released detector tracks only. No metric,
seconds,human-gold,physical safety,true3D,foundation or submission-ready claim.
Reserved data stay closed. No Stage5C or SMC.
