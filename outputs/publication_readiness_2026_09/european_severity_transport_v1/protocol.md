# Frozen Severity Transport Diagnosis

## Material Passport

Parent d42b8c40 is verified locally and on GitHub. The prior turn made progress:
144 new heads /288,000 updates completed; magnitude and tail gates failed.
This is fresh arithmetic/gradient diagnosis of cached_verified frozen models
and already exposed source-development labels, not a new training experiment
or independent confirmation. No checkpoint, forecast, role or policy changes.

## Question and Fixed Scope

Does the failed severity auxiliary concentrate excess error in a few
recordings/tracks, does its fitting gradient concentrate by recording, and
is excess error associated with a coarse fitting-feature support proxy?
Inspect all144 heads: six assignments, three seeds, full/motion-only and four
held source localities. Retain original and ordinary auxiliary comparators,
all-known and positive-disagreement populations. No favorable fold removal.

For each known held row compute (new_H_E - true_H_E)^2 minus
(comparator_H_E - true_H_E)^2. Preserve positive and negative row excess
separately, then group sums by recording and (recording,agent) track.
Report signed MSE, both mass totals, top1/top5 mass share and concentration
ESS. This grouping is accounting, not independent-sample estimation.
An improvement inside a recording must not cancel worsening before its
positive/negative mass is reported. Unknown targets remain unknown.

Use the unchanged easy label and report easy/outside-easy partitions.
No realized easy/error label enters prediction, the radial score or its cut.
Use only known fitting rows and original equal-locality weights to set the
95th percentile of RMS standardized causal features. Apply that cut to held
features; also report the fraction of features above absolute z=8. The model
does not clip those features. This radial proxy is NOT a conditional-density
or reliable out-of-distribution detector; low radius can still lack support.

On each saved final fixed fitting batch (256 draws, repeats retained), compute
shared-encoder gradients for ordinary BCE, H/mean_H-weighted BCE and the
easy-harm component of the unchanged cost loss. Each recording's masked loss
keeps the full-batch denominator. Verify group vectors sum to the total.
Report recording L2-norm concentration, cancellation and signed projections.
Norm masses are not additive causal influence; signed projections may be
negative. No optimizer step, training-path inference or per-window independence.

## Descriptive Decision Rules

No scientific pass/deployment gate is introduced. To route the next check:
flag concentrated held error if more than half of worsening full/original
views put at least50% of positive row excess in one recording. Separately
flag concentrated fitting gradients if more than half of full views put
at least50% of weighted-BCE recording gradient norm mass in one recording.
Only both flags together motivate investigating a recording-influence repair;
they do not establish causation or authorize dropping recordings.

Flag radial association if, in more than half of worsening full views,
the radial-outside positive-excess share exceeds its known-row share by>20pp.
This motivates support/context investigation, not proof of domain shift.
Keep all underlying counts and motion-only comparisons. Missing mass is
undefined, not zero evidence of improvement. No resampling-based significance
is claimed for these dependent diagnostics. Retain prior bootstrap results.

## Runtime, Reproducibility and Boundaries

Native arm64 CPU4/interop1/workers0, no new training. First complete one
registered group as a real diagnostic pilot, then resume remaining groups.
Save per-group immutable receipts and heartbeat. Preserve10GiB free space.
Check parent hashes before and after, and replay all144 views. CREATE queue
inspection is read-only; no existing job modified or new remote job submitted.

The inherited8 observed/12 predicted annotation steps and pixel/detector
labels remain unchanged. Independent selection/calibration/confirmation stay
closed. No metric/seconds, human-gold, physical-safety, true3D, foundation,
new trajectory gain or submission-ready claim. Stage5C/SMC off.
