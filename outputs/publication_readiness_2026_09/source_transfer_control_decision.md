# Exposed-Source Transfer and Matched Modality Control

## Material Passport

Prospective registration before new control fits or new held-site scoring.
New mask continuations/inference are `fresh_run`. The completed RGB continuation,
all inherited parent weights and source inputs are `cached_verified` only after
hash/schema checks. Main selection/calibration/confirmation remain `not_run`.

The prior training-only registration remains immutable and scored no held rows.
This separate diagnostic admits source-site scoring only after all control fits
finish. Bookstore has already been explored in this project. Excluding it from
these model fits does not restore an untouched confirmation role.

## Question and Fixed Comparison

The preceding three RGB cosine branches gained0.2533% on their full training
complement, while constant continuation remained negative. Does the repair
transfer to the excluded source site, and does RGB help beyond geometry and
image coverage? Do not interpret a smaller training loss as a method contribution.

1. Keep the same15430 complete stationary-history source training rows outside
   bookstore, and all6944 complete bookstore queries for this diagnostic.
   OriginalSDDtrain40 only,8-to12 atstride12/+144rawframes. Main task unchanged.
2. Continue three verified ADE/mask-only step2000 parents under both registered
   schedules to step10000. Six new branches/48000 new updates. Identical batch64,
   sampler state, feature normalization, AdamW/clip, decoder, capacity and
   checkpoint milestones to the previous RGB run. Reuse all six RGB endpoints.
3. Parent modality metadata remains truthful. A separately versioned explicit-arm
   trainer preserves the frozen RGB implementation; exact RGB equivalence is
   tested, as are mask invariance and resume. No hidden checkpoint relabelling.
4. Evaluate all18 unique predictor states: six2kparents and twelve10kendpoints.
   Every arm/schedule/seed is reported. No best-step/seed/modality selection.
   Each prediction producer excludesbookstore; weights and preprocessors stay frozen.
5. Primary diagnostic is window-average parent-normalized ADE onbookstore.
   Report FDE, native annotation-pixel error, training-defined hard slice,
   zero-CV absolute harm, tail error, per-recording/per-agent results and actual
   changed-prediction rate. Zero-denominator easy percentage remains undefined.
6. Secondary diagnostic retains the old same-arm/seed/source-excluded classifier
   threshold0.9 unchanged. This is not new gain/harm learning or risk calibration.
   Distinguish requested gate rate from actual changed predictions. No tuning.
7. Fixed contrasts: endpoint versus same-arm parent; RGB versus mask at the same
   schedule/budget; each model versus stationaryCV. Report all contrasts, not a
   retrospectively selected winner. Future-informed binary oracles are diagnostic.
8. Three-seed ranges are descriptive. Use2000 paired recording-block bootstrap
   resamples with fixedseed38113 after averaging seed errors per query. This is
   conditional uncertainty across seven recordings of one already-explored
   physical source site, not a scene-level generalization CI or independence proof.

## Leakage and Interpretation Boundaries

Only train contributes feature normalization, loss scale and hard cutoff.
Predictions see past features/images/frame restoration, never future targets,
goal endpoints or held-label statistics. Image coverage-only control still
receives geometry, observed rotation and masks; it is not a no-context baseline.
Missing future labels retain the prior complete-cohort diagnostic definition;
they are not reclassified as negatives or used to infer scene membership.

If gains disappear outside fitting rows, the training repair is insufficient.
If RGB does not beat its matched control, do not attribute improvement to images.
If a small gain survives, it still needs repeated source-scene support and the
separate joint-intervention/calibration study. No outcome alone promotes a model.
Offline supplied/interpolated annotations are not strict sensor-as-of or human
intention gold. No metric/time equivalence, true3D or foundation claim.

## Runtime and Verification

Local nativearm64TorchCPU4/inter-op1/workers0 is reasonable: the preceding matched
48k continuation took about40minutes with under2GiB observed processRSS. Current
disk has67GiB free. CREATE has only a saved authentication blocker; remote assets
and scheduler state are unknown, and no remote job is invented or duplicated.
Short100-update timing pilot belongs to the registered budget. Atomic checkpoint
and heartbeat every200steps; optimizer/sampler/Torch RNG resume.

Verify24 new training milestone forecasts,18 held forecasts, unchanged parent/RGB
artifacts on completed resume, and paired sampler streams across both modalities
and both schedules. Retain negative results. No Stage5C execution orSMC.
