# Frozen Readout Data Card

## Material Passport

Cached_verified source-development lineage from european_support_fractional_v1.
This round first performed fresh post-hoc label partitioning, then registered
matched frozen readouts. No data split is relabeled as fresh confirmation.

For each of six source assignments, source A provides the forecast/feature
chain. Within four B localities, three fit the original encoders and all new
preprocessing/readouts; the fourth is excluded throughout. Seeds 17/29/43 and
both full/motion-only forecast pairs are retained. The 12 source localities
are development-exposed. Six opened selection localities are unused this round;
12 reserved calibration and 6 confirmation localities remain closed.

The easy label uses a positive-CV-error training 25th-percentile cut. A zero
easy-harm target includes outside-easy rows as well as harmless easy rows;
the membership diagnosis keeps these separate. The target is a cost label,
never an inference-time selection mask. Future labels remain outside inputs;
central velocity, test goals and held normalization are prohibited.

Four held localities per assignment are resampled 3,000 times after averaging
three seeds within locality. Repeated source assignments and overlapping
windows do not create independent scenes. Bootstrap intervals are exploratory
and unadjusted for multiple comparisons. Detector-derived labels are not
human gold. Image pixels/annotation steps are not verified meters/seconds.
Private tracks, features, histories, frozen encoders and new weights stay local.
No policy evaluation, Stage5C, SMC, true3D or foundation claim.
