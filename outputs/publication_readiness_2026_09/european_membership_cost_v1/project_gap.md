# Remaining World-Model and Paper Gap

## What This Round Adds

A completed, resumable native-Torch experiment separates event membership
from conditional harm severity. The fixed-probability contrast is positive,
but the registered stronger-comparator cost gate fails. This is useful negative
component evidence, not a new deployable forecasting result. Training/metrics
are fresh_run; source arrays and frozen models are cached_verified.

## What Is Still Missing

1. Reliable expected intervention costs across source localities. Membership
   probability improvement has not solved cost magnitude or tail capture.
2. A fixed-forecast policy comparison demonstrating useful gains while retaining
   the approved easy-case tolerance. This round has no new policy evaluation.
3. Demonstrated advantage of scene-joint decisions over matched independent
   decisions, beyond assembling JEPA/Transformer/risk modules.
4. Independent scene-level calibration and confirmation. The reserved roles
   remain unopened; source-development bootstrap is not a risk guarantee.
5. Strong public-method comparisons and a contribution that goes beyond known
   cost-aware deferral. See [literature position](literature_position.md).

## Shortest Next Step

Do not scale the model or search another deployment threshold yet. Attribute
the failed product estimator into membership, within-easy severity and their
cross term using the frozen source predictions. Explicitly label any true-E
substitution as an offline diagnostic, not an achievable model. Determine
whether a controlled weighted-objective repair is justified before fitting.

If no repair beats the original cost model in development, retain the negative
result and narrow the claim; do not consume independent confirmation to select
a favorable variant. The long-term research objective remains active.

## Boundaries and Operations

CREATE was queried read-only this round; no jobs submitted or modified. Its
M3W artifact path remains unverified. Local CPU resources were sufficient.
Checkpoints, raw data and per-row outputs are private; public code/configs,
aggregate metrics and reports support reproduction. Routine audits are
delegated; formal submission still requires the user's final confirmation.

Eight observed/twelve predicted annotation steps, detector-derived pixels.
No metric/seconds, human-gold, physical-safety, true3D or foundation claim.
Historical Stage35/37 promotion is not restored by this component experiment.
Stage5C/SMC remain off. Status: not yet submission-ready.
