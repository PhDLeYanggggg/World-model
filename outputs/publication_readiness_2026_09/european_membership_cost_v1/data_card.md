# Membership-Conditional Cost Data Card

## Material Passport

Cached_verified source-development arrays and forecasting lineage. New
conditional cost fitting/evaluation is fresh_run and complete. Independent
selection, calibration and confirmation are not_run this round.

Source-A producers exclude B. Three B localities define preprocessing, loss
scales, class prevalence and the easy cut; a fourth locality is excluded from
all fitting. Six ordered assignments and three seeds are retained. These are
historically exposed source-development localities, not fresh final-test data.
Six selection, twelve reserved calibration and six confirmation localities
remain unused. Observations8/predictions12 are annotation steps, not seconds.

Inputs contain past context and causal producer rollout diagnostics. Realized
CV/reference/candidate errors and easy membership are only labels. Unknown
rows are excluded from fitting. The frozen membership model excludes the held
locality, and its probabilities are never second-stage fitting inputs. No
future endpoints, central velocity, held normalization or test endpoint goals.

The inherited event is 0 < CV_error <= fitting positive-error 25th percentile.
The conditional experts retain zero-harm rows within both strata. The constant
composition uses fitting prevalence, never held class frequency. Score bins
come from fitting predictions. Labels are detector-derived, not human gold.

Three seeds are averaged within locality before3,000 resamples of four
localities per assignment. Overlapping windows and repeated assignments are
dependent. Intervals are exploratory and not multiplicity-adjusted. Full and
motion-only positive-disagreement populations differ. No metric/seconds,
physical safety, true3D or foundation claim; Stage5C/SMC off.
