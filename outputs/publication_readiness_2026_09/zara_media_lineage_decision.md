# Zara Fit-Recording Media and Annotation Lineage

## Material Passport

Source-input repair after negative appearance/support trials, not new training.
Only already-approved fit recordings Zara01/02 are opened. Both belong to one
physical Zara scene. No development, risk-calibration or confirmation source is
opened or reassigned. The8-observed/12-predicted parent task stays unchanged.

## Fixed Source Checks

Read original spline annotations, stored obsmat rows, AVI header, supplied
H/H-old/H-cam matrices and upstream loader/UI documentation. Check declared
spline/control counts, monotone control times, stored one-based spline IDs and
canonical-cache identity. Keep every unmatched stored row visible.

For source reconstruction, compare the finite metadata conventions of stored
frame minus0 or1, rawcenteredxy/imagexy/imagerowcol and every supplied matrix.
Use linear interpolation within source control bounds only. No fitted matrix,
temporal lag sweep, future model error, learned goal or per-test decision.
Report all conventions; a small residual is numerical provenance, not proof of
physical scale, capture timing or point semantics. Fixed comparison tolerance
1e-5 nativeunits reflects printed6-decimal scientific values; also report max,
median and every unmatched count. Do not force a match if none exists.

Each interpolated row records its latest contributing spline-control frame.
Count complete8-past histories and how often any contributing control lies
after the current query, separately from ordinary timestamp-only causality.
Source interpolation is retrospective annotation construction; it must not be
silently called verified sensor-as-of input. Do not silently filter these rows
or change existing labels/protocol to resolve this issue.

Count8/12 support as an audit only; future availability never selects image
inspection queries or inference features. Raw future positions are never
extracted into the proposed input cache. If a unique row-replay mapping exists,
it may motivate a separately recorded past-image correspondence check, not
immediate full model admission or a metric/seconds claim.

All row lineage arrays stay in ignored local data. Public reports contain only
aggregate counts, source hashes and limits. Original datasets, models, reports
and metrics remain unchanged. No Stage5C, SMC or new deployment.
