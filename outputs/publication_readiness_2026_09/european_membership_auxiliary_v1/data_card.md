# Auxiliary Membership Cost Data Card

## Material Passport

Cached_verified source-development features and producer lineage; actual
new-fit/readout status is recorded separately in completion/compute receipts.
Source-A producers exclude B. Within B, three fitting localities define
normalization, costs, RMS scales, easy cut and priors; a fourth is held out.
These are previously exposed development localities, not fresh test scenes.
Independent selection/calibration/confirmation remain closed.

The main inherited protocol observes8 and predicts12 annotation steps.
Inputs are causal history/context and producer rollout diagnostics. Future
CV/reference/candidate errors and membership are only loss/evaluation labels.
No future endpoint input, central velocity, held normalization or test-goal
construction. The auxiliary prediction is not stacked as a training feature.

E=1(0<CV_error<=fitting positive-error25th percentile). Zero CV is outside E;
unknown labels are excluded from all sampling. Candidate-reference positive
error is the harm target, not physical collision risk. Inherited source
hashes and row order are checked at registration, fitting and verification.

Three seed means per locality precede3,000 resamples of four localities per
source assignment. Overlapping windows/repeated roles are dependent and
the intervals are exploratory, without multiplicity correction. Full and
motion-only positive-disagreement subsets differ. Images/trajectories/cache
are not uploaded. Detector-derived image pixels and annotation steps only;
no metric/seconds, human-gold, true3D or foundation claim. Stage5C/SMC off.
