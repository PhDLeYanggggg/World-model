# Execution and Provenance

## Scope

Registration `77e610eb` was pushed before the first new diagnostic readout. Both
upstream cross-moment analyses and their forecasts, checkpoints, decision banks
and producer lineage were hash-verified. New work is fresh error accounting and
evaluation, not new neural training. Model assets are cached_verified.

Local pilot PID 63375 completed at 12:47:17 UTC on 2026-09-25, with one group taking
8.975s after asset checks. Batch continuation PID 63413 completed at 12:50:14;
17 additional groups took 158.678s. Fitting-mode PID 63669 completed at 12:53:43;
18 groups took 166.322s. These are phase-local timings, not full research time
or training time. Native arm64 CPU4/inter-op1/workers0. About 25GiB remained free;
the run preserved a 10GiB hard guard. No large cache was materialized.

Both modes save immutable per-group receipts and an identity-bound completion
manifest. Resume checks completed groups before reuse. A final batch resume
was also run to verify that no analysis group needed recomputation.

## CREATE

Fresh authorized read-only queue check completed at 12:35:54 UTC, return code 0,
zero visible task rows. No job was submitted or modified. This is not evidence
that historical CREATE jobs completed or that remote artifacts do not exist.
The private query receipt SHA-256 is
`b57edb0ebbbd459bfc52173b500cf7d1a71c8d7389205f69531c8f5d47c5d9e2`.
Specific remote M3W artifact directories were not inspected this turn; local
assets were sufficient and no unrelated remote project was scanned. This
diagnostic is CPU arithmetic, so there was no reason to submit a GPU job.

## Verification

144 coordinate-error arrays, 72 saved causal decisions, 396 original parent
metrics, 3,312 metric reductions and 288 independently reduced ledgers passed.
All 288 support slices include the fixed locality roster. Three seeds and 3,000
paired locality-bootstrap draws per view are conditional development evidence.
282 tests across 44 scoped files passed; full legacy suite not run. Figure was
visually checked; the legend was moved outside the data panels without changing
any analysis. Tests and source/output hashes are bound in the completion receipt.

## Git Safety

Only this experiment's code, configuration, aggregate metrics, SVG and reports,
plus the project README/results/state updates, are included. No raw data,
private group archives, checkpoints, feature/latent caches or environment are
uploaded. Existing unrelated 3019 staged changes are left untouched; their raw
index-diff digest is checked before and after the scoped commits.

All results remain opened-development image-pixel obs8/pred12 rawstride12.
No threshold selection, reserved-role access, deployment, Stage5C or SMC.
