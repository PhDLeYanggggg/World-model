# SDD Step-Window Bridge, Not an Auxiliary Training Result

The previous information probe has a one-direction start signal, not robust
bidirectional transfer. More classifiers on the same two sites are not the next
experiment. The blocking scientific choice is the auxiliary sampling/source-role
contract, not a blanket lack of permission to work with SDD or a Torch failure.
The user has previously authorized SDD work and delegated the route; the current
frozen 8-to-12 primary contract nevertheless does not define this new auxiliary
arm. A focused question now distinguishes stride12 auxiliary training, native
stride1 auxiliary training, or no auxiliary training. No answer is inferred.

Prepare the reusable geometry bridge independently of that choice. Read only
the original Stage21 train roster of 40 videos and bind the corrected annotation
manifest. Do not read original SDD validation/test labels or change ETH/UCY roles.
All these SDD sources retain historical exposure; none is new confirmation.

Support stride1 and stride12 as diagnostic configurations only. They are members
of the previously audited stride set, not a selected winning training interval.
No physical elapsed-time equivalence is asserted. Keep original source pixels,
occlusion/generated provenance and the same past-only coordinate transform and
seven causal baseline definitions used by the existing 8-to-12 model.

Build per-video index files from eight available non-lost past states only. Ego
queries are pedestrians; neighbors may have other observed source types. Keep
occluded and generated states with provenance, no human-gold or sensor-as-of claim.
Only frame/agent/position data construct motion, never old derived velocities,
time_s, central differences or future endpoints. Future support is a separate
label API and mask, not a query or neighbor eligibility condition.

Reuse the existing 476-column geometry schema. For each train recording/stride,
check 64 evenly spaced past-index queries (or every query if fewer). Use an
untrained, zero-initialized existing Torch head to verify shapes and exact CV
forward equivalence with explicit absent-image masks. This is an interface check,
not training, accuracy or multimodal success. Verify masked loss semantics on
synthetic data, and real future mutation/truncation on the first/last sampled
queries. Save only lazy indices and receipts, not full materialized episodes.

Run one-record pilot, then all 40 records if resources permit. Local disk currently
has approximately 82 GiB free; indices are expected to fit. Native arm64 CPU4,
interop1/workers0; record-level receipts permit verified resume. No weights need
checkpointing because there are zero optimizer updates. Do not repeat immutable
full video decoding or claim image alignment beyond the earlier source audit.

After a sampling decision, register the actual auxiliary/control training study
separately. Preserve the primary, scientific roles, negative evidence and closed
confirmation. If there is no decision, suspend auxiliary fitting rather than
inventing another fit-only parameter sweep. CREATE access and physical time/scale
remain separate gaps. Stage5C and SMC stay disabled.
