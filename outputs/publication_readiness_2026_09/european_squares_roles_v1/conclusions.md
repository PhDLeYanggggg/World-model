# Frozen Locality Roles, Restricted Training Admission

2026-09-24. Result source: fresh_run. Source dependencies are cached_verified.
The assignment reproduces exactly before any prediction-error readout or fit.

The user delegated routine scientific audits and evidence-based data-role
decisions. I have converted the completed source checks into a fixed, enforceable
training boundary, not declared all records independent or opened every split.

## Allocation

The conservative 10 km sensitivity merges Bevagna/Montefalco (IDs 7/45), in
addition to Trencin 67/68 and Brzesko 104/105. The 39 nominal squares become
36 provisional locality groups. Source location distance is used only to keep
related recordings together, never to calibrate trajectory coordinates.

| Reserved purpose | Locality groups | Recordings | Raw rows | Scoped tracker IDs | K8/stride12 past-supported targets |
|---|---:|---:|---:|---:|---:|
| Source training, opened | 12 | 163 | 51,526,905 | 175,650 | 31,111,802 |
| Model selection, closed | 6 | 28 | 6,179,261 | 23,584 | 3,553,348 |
| Risk calibration, closed | 12 | 102 | 80,910,623 | 259,713 | 48,366,545 |
| Confirmation, closed | 6 | 83 | 13,755,277 | 55,668 | 7,669,936 |

These are overlapping support counts, not independent sample sizes. Repeated
seasonal data remain within their locality role. Six-group support strata and
one fixed SHA256 ordering choose the allocation; there is no seed search,
future-completeness filter, outcome balancing or post-score role reassignment.

The role manifest hash is
`858065b8a14f15a8741cbd9b0f24e5de595a02512b2552709d37b6eeb5e88435`.
Every recording binds its raw parsed-row hash and locality. The training accessor
rejects model-selection, calibration, confirmation, missing and ambiguous records.
The six role tests verify this, including a reserved record with an erroneously
true access flag. All 213 reserved recordings are also denied during manifest
construction. Rebuilding the manifest gives identical bytes.

## Precisely What Is Admitted

Only a retrospective released-detection-track learning task is admitted now.
It uses unsmoothed raw box centers in image pixels and an explicitly separate
external grid: eight observations and twelve requested outputs, raw stride12.
There is no verified common physical clock with SDD and no metric ground plane.
The primary SDD protocol and its historical limitations are unchanged.

All input coordinates, velocities, neighbor visibility and eligibility come
from the query prefix. Future coordinates and validity are separate supervised
labels; neither full-track survival nor future support selects targets. The
source cannot currently support claims about sensor-online human ground truth,
true physical collision safety or human-gold labels. No media was acquired for
this source intake; a trajectory cache is not a new multimodal model result.

All reserved prediction errors remain closed. Opening a later selection,
calibration or confirmation readout requires a versioned frozen experiment and
producer chain first. DroneCrowd stays separately closed confirmation. A dozen
reserved localities do not automatically yield a tight 2% population bound.
The exact-zero-CV harm allowance remains zero; positive-easy tolerance remains2%.
If calibration lacks support, the policy must reject/fall back, not change those
limits. No deployment promotion, Stage5C or SMC follows from these roles.

Bounded prior-exposure and partial-clip checks do not prove universal site or
model independence. Unknown remote assets and detector pretraining are not
silently treated as clean. These limitations remain attached to the source.

[Frozen registration](registration.md), [role manifest](roles.json),
[partial-clip review](../european_squares_overlap_v1/conclusions.md),
[prior-exposure scope](../european_squares_exposure_v1/conclusions.md).
