# Data Card: Dual-Event Bridge

Source assets are reused with SHA-256 and schema/role verification; this round
does not convert a new dataset. Fresh work is cost-head training, inference,
and the subsequent frozen readout. EuropeanSquares released detector tracks
are box-center image pixels, not human-gold annotations or calibrated geometry.

The twelve already-opened source localities form three disjoint four-locality
rosters. Every producer A's forecaster, motion floors and old_stop policy exclude
controller B. New controller heads and normalizers fit B only. The six ordered
A/B assignments use all three seeds; the assignments are dependent.

The 38,102 model-selection rows at 7,087 queries across 28 recordings and six
localities were already exposed in the preceding study. They are reused
development/model-selection evidence, never new test or confirmation data.
21,434 rows have complete future labels, 15,820 partial labels, and 848 no future
labels. All past-indexed rows receive predictions; unknown labels are not used
for fitting or numerical error estimates. The endpoint has separate support.

Eight observed and twelve requested annotation steps use the frozen stride-12
adapter. Complete-history target agents and up to eight nearest aligned-history
neighbors enter the encoder. Incomplete visible histories remain in the raw
source cache but are not silently described as fully encoded all-agent context.

No future endpoint, central velocity, future-derived support mask, test goal or
readout statistic enters the new feature API. CV-easy membership uses future
errors as a supervised label only, not as an inference-time gate. Risk targets
use A's training-only easy cutoff; new feature normalizers use B only.

Twelve risk-calibration and six confirmation localities remain closed. The
existing 84 truncation tests and source replay are cached_verified evidence,
not new raw-data tests. No metric, seconds, physical safety, true-3D or foundation
claim. No new datasets, scene images or endpoint-goal construction this round.
