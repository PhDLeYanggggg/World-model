# Project Gap After the Fractional-Harm Test

## What This Round Adds

A matched 144-head, three-seed Torch experiment now separates ranking lift
from reliable expected-cost prediction. Supported full harm ranking improves,
but the registered magnitude gate fails. This is useful mechanism evidence
for the research record, not a new deployable selector or stronger forecast.
The original mean controls and all adverse roles are retained.

## What Still Prevents a Strong Main Claim

- Reliable conditional and selected-set harm magnitude under locality shift.
- A policy benefit supported by fixed risk constraints, not just an AUROC gain.
- Main comparisons on truly independent calibration/confirmation roles after
  model and protocol freeze; current source views are already development-exposed.
- Strong public-method controls, ablations and scene-level uncertainty for the
  eventual final method, with no reinterpretation of overlapping rows as scenes.
- A compact method claim beyond stacking JEPA/Transformer components or loss terms.
- Verified data/time/geometry and label-quality limits; current images and
  detector-derived labels do not establish physical safety or metric 3D.

## Shortest Next Action

The additive decomposition and all 144 checkpoint replays are complete. The
zero-easy-harm partition dominates 37/39 full worsening views; distinguish
outside-easy rows from inside-easy harmless rows before the next repair.
Then register a single controlled frozen-representation magnitude
readout test only if its mechanism is distinct from the previous calibration
and reference-protection experiments. Do not open a new C policy readout with
the failed fractional head. Keep the six role assignments and three seeds.

Local execution remains appropriate for these small heads. CREATE remote M3W
inventory is still not_run because the project-specific directory is unverified;
the simulation project's active work and connection restrictions are untouched.

## Status

Research goal remains active. Deployment unchanged; no new policy assessed.
Not yet a CCF-A/CVPR submission candidate. Stage5C and SMC remain disabled.
Formal submission still requires author confirmation. Obs8/pred12 annotation
steps, raw-frame supplemental horizons, image pixels, no seconds/metric claim.
