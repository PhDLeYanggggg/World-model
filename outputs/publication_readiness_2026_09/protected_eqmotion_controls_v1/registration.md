# Full EqMotion: Matched Protected Motion Extension

Registered 2026-09-24 before new forest fitting and this comparative readout.
Existing source-development EqMotion/neural and motion/Transformer results have
already been observed. This is not prospective independent confirmation.

## Material Passport

- Mode: code experiment, fixed source-development comparison.
- Data: 175,756 past-eligible pedestrian windows, 33 recordings, four SDD sites.
- Protocol: eight observed and twelve predicted native annotation steps,
  raw stride12, annotation pixels; no verified seconds or metric claims.
- Fitting role: source-only cost supervision; all sites remain design-exposed.
- Compute: native arm64 CPU4, interop1, workers0, atomic checkpoints and resume.

## Asset Correction and Reuse

The previous motion-control handoff erroneously listed EqMotion's pair-excluded
producer bank as missing. Eighteen producers and their cost archives already
exist, as do twelve full-forecast bounded-fraction neural cost heads. The current
EqMotion cost-refit preflight validates 841 bindings. Do not repeat the 7.75-hour
producer training. Preserve the original registration and attach the correction.

Reuse those twelve neural heads after exact feature, label, known-support,
preprocessing, seed, budget and sampler checks. Reuse the previously verified
four-site causal/Transformer comparison. Fit **only twelve** full-EqMotion forest
heads with 128 trees, depth16, leaf64, max_features1/3 and sample weights equal
to each paired neural head's actual 768,000 draws (zero-disagreement rows have
zero objective mass). Older ramp/uniform EqMotion forests are not substitutes.

The outer cost fit excludes its held physical site. Forecasts producing each
training cost additionally exclude that row's site. Cached predictions contain
IDs and forecasts only; targets are in separate supervision archives. Source
neural preprocessing is not fitted again. Future labels may supervise only train
rows; they cannot change inference population, scores or selection.

## Fixed Comparison

Compare full EqMotion with full Transformer and every six registered causal
alternative, using both neural and forest cost heads. No best action, head,
threshold or seed is selected. All heads use the same 356-feature cost interface,
bounded-fraction objective and moving/nonzero-disagreement support. Strict means
predicted benefit > harm and harm <=0.1*benefit. Net-positive-only and uncontrolled
EqMotion remain diagnostic controls.

All twelve forests complete before new decisions. Freeze all per-view neural/
forest decisions and all paired matched counts before loading outer target
arrays. Neural score/strict-bit reproduction must match its existing EqMotion
archive exactly. Population and common easy/hard cutoffs come from the completed
protected-motion experiment, not from an outcome-selected EqMotion subset.

Primary descriptive contrasts are EqMotion minus each comparator under strict
protection, separately by cost-head family. The second control matches the smaller
strict intervention count per fold/seed, ranking only within each arm's strict
pool by predicted net gain, breaking ties by global row ID. This is an offline
allocation diagnostic, not an online deployment policy.

## Estimands

Retain mean of four scene-relative available-point ADE gains over causal CV;
report FDE, complete paths, hard, positive-easy, zero-CV harms, selection counts,
unknown/partial-label interventions, tails, and full-grid missing-label gain
bounds. Include both arms' easy and hard results for matched counts, rather than
reporting a gain difference alone. Three seeds, mean seed errors (not trajectory
ensemble), 3,000 paired physical-site bootstrap draws. Four development sites
and nominal multiple contrasts cannot certify generalization or subgroup safety.

No new EqMotion architecture, JEPA contribution, multimodal contribution or joint
interaction result is claimed. The author core is a fixed adapted predictor, not
a reproduction of its published best-of-many protocol. Histories are offline
annotation histories; temporal indexing does not resolve interpolation provenance.

## Runtime and External Boundaries

A single fixed 16-tree pilot continues unchanged to128 trees, then all twelve
fold/seed fits. No slowness-based sample reduction. Keep local checkpoints and
events. Replaying existing source data is cached verification; new forests and
comparative evaluation are fresh runs. No DUT retuning or new readout. DroneCrowd
confirmation remains closed. Stage5C and SMC remain false.

CREATE was rechecked serially with the existing approved read-only connection on
2026-09-24. Authentication failed on the first query (exit255); current queue
state and remote M3W assets therefore remain unknown. No credential changes,
submission or protected simulation-project access occurred. This modest missing
forest bank is suitable for the already verified local runtime.
