# Conditional Quantization-Only CV Hypothesis

The completed fit-only precision audit finds integer-pixel lattice agreement
under supplied H, but not a verified measurement-error model. Before proposing
another start head, test whether hidden dataset-local constant velocity alone
could explain each fixed stationary history and its future labels.

For each of the same 365 fit rows, invert the supplied H and round the numerical
reprojection to the nearest integer pixel. Use closed boxes of half-width0.501
pixels (half a pixel plus the already declared0.001 arithmetic tolerance).
This is an optimistic conditional quantization model, not physical uncertainty.

Fit no model. Solve a four-variable feasibility LP for x0,y0,vx,vy using all
eight observed and twelve target positions on the same native-step times.
Under a fixed projective denominator sign, image-cell inequalities are linear
in these four variables even though the projection itself is projective. This
tests constant velocity in the original coordinate plane, NOT linear image
velocity. Verify feasible solutions by reprojecting them into all20boxes.
Keep solver failures, noninteger lineage and chart/sign issues not_run.

Report rows/agents/runs and CV-error share for feasible versus incompatible
trajectories, per scene and return-to-origin subset. Future labels enter this
oracle diagnostic only. No inferred velocity or feasibility flag is supplied
to any model, policy or gate. Do not select a threshold or relabel data.

Infeasibility rejects this particular hidden-CV/rounding explanation, not all
annotation error or all latent dynamics. Feasibility proves neither real motion
nor a useful predictor. No new training, calibration, deployment, metric/seconds,
primary-protocol change, Stage5C or SMC. The native-ADE/FDE question remains open.
