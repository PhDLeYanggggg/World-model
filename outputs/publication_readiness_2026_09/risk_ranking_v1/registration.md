# Fixed-Count Risk Ranking Controls

2026-09-22, registered before this diagnostic's new outcome readout. Prior
forest/neural outcomes are already exposed. This is a follow-up development
diagnostic, not a prospective independent test or a replacement primary gate.

## Question and Fixed Scope

Does the forest's observed protection remain distinctive when the neural head
ranks by harm relative to benefit instead of absolute net gain, at exactly the
forest's existing switch count? Do not train another model or tune thresholds
before answering this simpler control. This does not itself create a method
contribution, independent calibration, external validation or joint decisions.

Reuse verified frozen ramp candidates, cost scores, source exclusions, 356
causal features, four development-exposed sites and seeds17/29/43. The original
forest superiority gate remains failed regardless of this outcome. Do not
promote uniform or any new control based on these development scores.

Eight observed / twelve predicted annotation steps at raw stride12; SDD pixel
coordinates. Raw-t50 supplement, metric/seconds, true3D and foundation claims
are not evaluated. Stage5C and SMC remain off.

## Fixed Choices Before Labels

For each site/seed, retain the frozen ramp forest strict count K. All four
ranking controls use the same past-only eligible pool: nonzero last observed
step and positive causal forecast disagreement. No future mask participates.

- forest_ratio and neural_ratio: ascending H/(G+H), equivalent to H/G ordering
  where G>0. G=0 receives maximal risk1, including G=H=0. No fitted epsilon.
- forest_gain and neural_gain: descending G-H.
- Ties use ascending immutable row id, not labels or row position.
- Each takes exactly K. No new threshold or risk multiplier is selected.
- Keep both parent strict choices as context. Neural strict need not have K.
- Forest ratio must equal its strict choices exactly; otherwise stop.
- Neural gain must reproduce the prior same-count control exactly.

Relative risk is invariant in ranking to a common positive harm multiplier.
This removes such a score-level effect at fixed count, but not nonlinear,
row-dependent conservatism. Counts are conditional on an already frozen forest,
not a validated or deployable per-scene operating budget. No deployment rule is
learned. Same count does not imply matched causal-displacement/risk mass.

## Readout

Fixed diagnostic contrasts: forest strict minus neural ratio (main diagnostic),
neural ratio minus neural gain, and forest ratio minus forest gain. Report all;
no winner selection. Use the unchanged equal-scene ADE primary accuracy
definition, FDE, hard/easy, worst-site/seed easy, complete exact-CV harm, seed
dispersion, tails and physical-site paired bootstrap3000/seed38113. Intervals
are conditional/exploratory across four exposed physical sites, not four new
datasets, not independent overlapping windows, not multiple-test-adjusted proof.

Keep unsupported labels visible, report selected incomplete/unknown support and
full-grid triangle-bound gain. Report choice overlap, both heads' conditional
cost errors on each fixed selection, causal displacement mass, temporal
smoothness and unchanged past-context excess-proximity proxy. Native pixel
proxies are not verified physical safety. No unknown easy cases counted safe.

Reuse unchanged empirical checks (positive CV gain, each-site/seed easy<=2%,
complete exact-zero-CV harm0) for descriptive diagnostics only. No new pass
gate replaces the failed forest-primary comparison or establishes calibration.

## Execution and Boundaries

Native arm64 local runtime, workers0. No new fitting; read cached scores only
after parent hashes/schema/replay checks. Freeze new decisions before accessing
future labels, retain identity/hash manifests and PID heartbeat, exact replay,
separate choice/reduction verification and scoped tests. Intermediate failures
must not overwrite existing immutable results. Private row arrays remain local.

The previous status turn was a status report, not new research progress. Current
priority is this causal ranking control; independent source admission and
calibration/confirmation roles, then joint-agent contribution remain higher-level
gaps. CREATE project/auth and queue are unverified; local score analysis does
not justify transferring data or starting a remote job. No scientific role or
risk-tolerance decision is silently changed.
