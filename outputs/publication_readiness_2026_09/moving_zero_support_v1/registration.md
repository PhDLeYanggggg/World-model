# Moving Zero-CV Support and Causal Neighborhoods

Registered before the new distances and raw-annotation case readout. Follow-up
to zero_atom_v1, not a new deployment candidate. Four exposed SDD sites, all
three frozen seeds/actions; obs8/pred12 stride12 annotation pixels. Unknown
labels and previous failures remain unchanged. No fitting or threshold selection.

The earlier raw-history audit already rejects numerical-noise and exact-past-CV
veto explanations for its harmed cases. The new experiment covers all seven
moving zero-CV windows and the actual current forest training rows. Distinguish:
rare relevant source support, coarse partition support, nearest-feature mismatch,
exact causal-feature aliasing, and supplied-annotation interpolation provenance.

For each of 36 source-only fits, use its exact known draws times D>0 weight and
restrict the diagnostic neighborhood to moving histories. Unknown labels have
zero support. Compare two feature banks: the 16 history-coordinate columns and
the complete cutoff-relative risk features, using only the frozen source-fitted
standardization. Query all held-source moving zero-CV cases plus 32 deterministic
metadata-hash selected moving controls per held site/action/seed. Controls are
selected without future labels. Outcomes group the diagnostic cases only.

Use direct float64 RMS Euclidean feature distance, exact equality and stable
source-id tie breaks. Report nearest event rank/distance, exact label aliases,
source label composition and distinct scoped-track counts among 32/128/512
neighbors. These are descriptive neighborhoods, not calibrated probabilities,
learned thresholds, independent episodes or Bayes-impossibility proofs. Near
neighbors are not identical contexts. Stored row details remain private.

Verify all seven raw histories against cached row identities and raw future
labels against released annotations. Check annotation-generated flags, control
bracketing, visible support and sampled/raw-grid CV consistency. Future controls
and label errors are retrospective provenance only and never inference inputs.
Preserve the supplied-annotation benchmark; do not remove cases or relabel data
roles based on this diagnosis.

All query/action/feature results and negatives are retained. Source identities,
per-view atomic receipts, PID heartbeat and exact replay are required. Start one
full-view local pilot to measure cost, then complete all views without sampling
away the seven cases. DroneCrowd remains closed, IMPTC quarantined. No metric,
seconds, physical safety, true3D, foundation or submission-ready claim. Stage5C
and SMC remain off. No deployment change or new neural forecast training.
