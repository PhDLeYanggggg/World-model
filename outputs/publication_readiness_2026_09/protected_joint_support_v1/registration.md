# Causal-Only Joint Opportunity Check

2026-09-22. Register before executing this check. Existing predictive results
have already been read; this is a development support diagnosis, not blinded
method discovery or a new accuracy experiment. No future target/mask arrays are
loaded. Hash verification may read file bytes, not consume them as model inputs.

Question: do the newer forest/log/square strict proposal pools offer meaningful
non-additive agent-pair opportunities under the previously fixed joint objective?
The original native joint control was null, mostly due sparse supported pairs
and weak coupling. The newer risk heads admit different pools and the forecast
action now uses a temporal ramp. Check the actual prerequisites before another
joint-model training or geometry-weight search.

Use all four existing development sites and seeds17/29/43, all33recordings and
20,932recording/frame queries per seed. Compare three fixed pools: forest ratio
(identical to forest strict), old log strict, new square strict. All use the same
ramp forecasts. Preserve the previous half-count budget, top-gain reference harm
cap, train-only cost scaling, median-past-scale neighborhood, radius fraction0.1
and pair weight1. Full-count selection remains structurally unique.

Report counts before any prediction-error readout: nonzero half counts,
supported non-additive edges, exact feasible-set size, product-cost range,
unary runner-up gap, any changed identities and meaningful predicted-objective
advantage. Enumerate all combinations for every non-additive opportunity up to
200,000 per query. Larger sets are explicit blockers, never subsampled or counted
as a null. Structural zero-product cases require no optimization. Compare
independent direct pair-sum arithmetic to the decomposition for enumerated sets.

This does not evaluate whether changed decisions improve ADE or safety. Do not
choose a pool, weight, threshold or a new training arm by diagnostic advantage.
No future labels, future validity masks, oracle choices or test endpoint goals
enter this computation. Reuse verified source lineage and past scene context.
Checkpoint each view/recording; replay hashes and aggregate counts exactly.

8 observed/12 predicted annotation steps, pixel only. No metric, seconds,
physical-safety, independent calibration, true-3D or foundation claim. No new
deployment, Stage5C, SMC or full trajectory training. Local CPU workload; CREATE
connection/project remains unverified and no remote job is requested.
