# Data And Asset Card

- Dataset: EuropeanSquares released detector-derived tracks, not human gold.
- Scope: the same twelve already-opened development source localities.
- Protocol: observed8/predicted12 at raw annotation stride12, image-pixel space.
  Not the earlier SDD raw-t50 protocol; no metric or effective-seconds claim.
- Cached row inventory:318,969 windows;193,705 complete,118,217 partial,
 7,047 with no future label. Counts are inherited from the verified source chain.
- A/B/C: disjoint four-locality groups per rotation. A fits the cached neural
  forecaster, floor and incumbent; B fits new heads; C gives development readout.
- Six role rotations, three seeds, two event targets reuse sources and rows.
  They are not36 independent datasets. Locality is the bootstrap unit.
- A-derived easy/hard thresholds remain unchanged. B-only feature statistics,
  supervised target costs and CV cost normalization. No C normalization.
- Both new arms have the same381 causal features: verified prior380 schema and
  a replayed incumbent decision from past inputs. Targets are not input columns.
- Future coordinates/errors: training loss labels on B or readout labels on C
  only. Missing ADE remains unknown. Endpoint FDE needs the actual last label.
- No test-endpoint goals, central velocity or future endpoint input. No new
  scene packs/prototypes; no claim of newly validated scene-image contribution.
- Existing forecasters and prior evidence: cached_verified with hash lineage.
  New controller training and comparative evaluation are fresh_run only after
  completion. Reserved selection/calibration/confirmation sources remain closed.
- Raw tracks, caches, checkpoints and private scheduler output stay outside Git.

The prior239 public artifact hashes, local/remoteHEAD d861eb06, native arm64
runtime and19.2GiB free disk were checked before preparation. CREATE's authorized
queue query succeeded read-only; no remote job/upload. The authorized M3W remote
artifact directory remains unknown, not demonstrated absent. This is not a local
training blocker and does not justify scanning another project's storage.
