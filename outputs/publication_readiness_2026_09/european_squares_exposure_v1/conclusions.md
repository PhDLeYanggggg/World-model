# Bounded Prior-Exposure Check

2026-09-24. Result source: fresh_run. This is not a universal non-exposure proof.

The pinned pre-intake repository state is
`5155154d78591af69ebfd61cd2adee71f7366ae3`. A case-insensitive literal search covers
435 aliases: dataset/record/paper identifiers, publisher code name, camera
references and all raw filenames. The specified README, research state, configs,
source, scripts and September publication-evidence scope contains 3,083 tracked
paths. Git's binary-excluding text search returns no matches.

The same aliases are absent from 91 directly scoped local experiment
`identity.json` / `run_identity.json` files. Their paths and hashes are retained.
The reviewed native Transformer and EqMotion registrations bind their source
training to the exposed SDD sites, not this acquisition. No model weights,
unknown future labels or predictor errors were opened for the alias search.

This does not inspect every historical Git revision, arbitrary binary or
untracked dataset, nested receipt, downloaded detector pretraining set or remote
model. Source aliases may also be omitted or renamed. Therefore zero matches
are evidence in a bounded scope, not permission to claim universal independence.
New-source training must not import an unverified teacher or pretrained asset.

## CREATE Asset Boundary

The existing `simulation model` task was asked for an exact M3W remote directory,
as authorized by the user. Its 2026-09-24 reply states that no such directory is
known in its records. It did not scan the remote home or inspect M3W assets.
Its fully synthetic simulation project, environments and outputs are not M3W
assets. Scheduler access working again does not locate the M3W project.

Remote M3W asset inspection is `not_run` because the exact directory is unknown.
No remote search, job submission, authentication change or interference with
simulation work was performed. This does not block the independently verified
local source cohort. The limitation remains in the admission report.

[Search receipt and identities](analysis.json),
[restricted role registration](../european_squares_roles_v1/registration.md).
